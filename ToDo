The ToDo List

-> durch längeres betätigen des + oder - zeichen soll der Timer schnell hoch oder runter zählen
-> Implementierung eines SettingsMenü
-> direkter eintrag der Zeit
-> Nach beenden soll auch wirklich alles beendet sein.
-> Timer soll aus der Datenbank heraus gestartet werden können


#Done

-> Vibrationsalarm hinzufügen
-> Wenn man auf Start drückt soll der Alarm auch aus gehen
-> man kann nicht erneut Alarm drücken ohne den alten zu beenden
-> wenn man anfangs gleich auf Stop drückt dann stürzt der Timer ab
-> Oberfläche muss besser angepasst werden
-> Datenbank zum abspeichern der Timern
-> Soundausgabe für den Timer
-> Verlassen nur noch durch doppeltklick möglich
-> Zeiteingabe für das Hinzufügen des Timers
-> Stop- / Resetbutton muss hinzugefügt werden

Infos:
test for longer button click...

 view.setOnTouchListener(new OnTouchListener() {
        @Override
        public boolean onTouch(View v, MotionEvent event) {
            switch(event.getAction()) {
            case MotionEvent.ACTION_DOWN:
                // Start
                break;
            case MotionEvent.ACTION_UP:
                // End
                break;
            }
            return false;
        }
    });